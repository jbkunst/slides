---
title: '<br><br>Qué es esto de R en la pega?<br><small>Una bienvenida al mundo de R</small>
<link href="https://fonts.googleapis.com/css?family=Bangers|Roboto+Condensed|Covered+By+Your+Grace" rel="stylesheet">'
author: Joshua Kunst, Dic 16
output:
  revealjs::revealjs_presentation:
    mathjax: null
    transition: fade
    css: styles.css
    incremental: true
    center: false
    theme: simple
    fig_width: 5
    fig_height: 4
    reveal_options:
      slideNumber: true
      controls: false
      mouseWheel: false
---

# Bienvenido {.section-title-white .center data-background="http://i.giphy.com/RbVOveSAHkk2A.gif"}

```{r, include = FALSE}
knitr::opts_chunk$set(message = FALSE, warning = FALSE, echo = FALSE)
library(tidyverse)
library(rwars)
library(highcharter)
```


## Nah! Que es R y Rstudio?

- R es un lenguaje de programación _wakatela!_
- Rstudio es un (IDE) una máscara para ser más productivo _yuju!_
- R es software libre, no se paga y está un poco de moda ahora
- 

## Workflow de <strike>trabajo</strike> vida del día a día

![DSflow](http://r4ds.had.co.nz/diagrams/data-science.png)

## Jerga

- Paquetes: Un paquete es un conjunto de funciones
- Script: Un archivo que contiene instrucciones en lenguaje R
- Joshua: El master de R por ahora hasta que los superen sus alumnos
- Ustedes: Alumnos ultramotivados a darle un cambio de aire a sus vidas

## R posee más de 8000 mil paquetes

![tidyverse](https://www.rstudio.com/wp-content/uploads/2015/01/Untitled.001.jpeg)

## Y que me importa a MHEE!! Que se puede hacer con ellos?

- Lectura de datos desde muchas fuentes
- Manipulación de datos, pa'rriba pa'abajo al lado y paentro.
- Gráficos kawais
- Presentaciones, aplicaciones web, documentos

__Y como?!__

## Testing

```{r}
swmovies <- get_all_films()

swdata <- map_df(swmovies$results, function(x){
  data_frame(
    movie = x$title,
    species = length(x$species),
    planets = length(x$planets),
    characters = length(x$characters),
    vehicles = length(x$vehicles),
    release = x$release_date
  )
}) 

swdata <- swdata %>% 
  gather(key, number, -movie, -release) %>% 
  arrange(release)


hchart(swdata, "line", hcaes(x = movie, y = number, group = key),
       color = c("#e5b13a", "#4bd5ee", "#4AA942", "#FAFAFA")) %>% 
  hc_title(
    text = "Diversity in <span style=\"color:#e5b13a\"> STAR WARS</span> movies",
    useHTML = TRUE) %>% 
  hc_tooltip(table = TRUE, sort = TRUE) %>% 
  hc_credits(
    enabled = TRUE,
    text = "Source: SWAPI via rwars package",
    href = "https://swapi.co/") %>% 
  hc_add_theme(
    hc_theme_flatdark(
      chart = list(
        backgroundColor = "transparent",
        divBackgroundImage = "http://www.wired.com/images_blogs/underwire/2013/02/xwing-bg.gif"
      )
    )
  )
```

